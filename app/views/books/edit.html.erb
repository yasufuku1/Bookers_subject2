<h2>Editing Book</h2>

<% if @book.errors.any? %>

  <div class="error_box">
    <h2><%= @book.errors.count %> error prohoboted this book from being saved:</h2>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div>
  <%= form_with model: @book, url:book_path(@book.id), method: :patch do |f| %>
    <div class ="book_box">
      <lavel>title</lavel>
      <%= f.text_field :title %>
    </div>
    <div class="book_box">
      <lavel>body</lavel>
      <%= f.text_area :body %>
    </div>
    <div class="book_box">
      <%= f.submit 'Update Book' %>
    </div>
</div>
<%= link_to "show",book_path %>
<span>|</span>
<%= link_to "Back",books_path %>
<% end %>
